pipeline {
     agent any
     stages {
        stage("Build") {
            steps {
                sh "npm install"
                //sh "npm run build"
                sh 'echo "Hello World"'
                sh ''''

                set +e

                ..

                '''
            }
        }
    }
}