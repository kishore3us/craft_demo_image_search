pipeline {
     agent any
     stages {
        stage("Build") {
            steps {
                sh "sudo echo 'Hello World'"
                //sh "sudo npm run build"
            }
        }
    }
}